apiVersion: 1

providers:
  - name: Default # A uniquely identifiable name for the provider
    folder: MetrANOVA # The folder where to place the dashboards
    type: file
    updateIntervalSeconds: 10
    allowUiUpdates: true
    disableDeletion: true
    options:
      path: /etc/grafana/provisioning/dashboards
